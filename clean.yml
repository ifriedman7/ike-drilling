---
- hosts: local
  connection: local
  vars:
  vars_files:
    - aws_keys.yml
    - vars.yml
  tasks:
    - name: "Delete VPC"
      ec2_vpc_net:
        aws_access_key: "{{ aws_access_key }}"
        aws_secret_key: "{{ aws_secret_key }}"
        region: "{{ region }}"
        cidr_block: "{{ vpc_cidr }}"
        name: "{{ vpc_name }}"
        state: absent
    